# 更新记录

## 1.0.1 · 2026-09-23

- 修复通过普通 HTTP 访问云端酒馆时，因 crypto.randomUUID 不可用导致瞬息初始化失败的问题。
- 创建作品、章节、分类及导入主题统一使用兼容编号函数；不支持 randomUUID 时使用 getRandomValues 生成 UUID v4。
- 保留已有数据与主题，提供新版单文件 HTML 预览。

## 1.0.0 · 2026-09-23

- 首个正式发布包，原生 SillyTavern 扩展，不依赖酒馆助手。
- 内置正文：邮票素笺、银河星系、庭前落英、小猫手账；小手机：简白、深灰、粉白、婴儿蓝。
- 移除开发阶段的默认主题与演示提示、自动模拟报错；旧作品阅读样式保持快照。
- 提供 momenttheatre 发布目录、ZIP、离线 HTML 预览与 GitHub 安装更新说明。

## 0.9.5 · 2026-09-23

- 正文主题预览统一使用固定标题及引号、加粗、斜体、删除线和普通正文示例。
- 银河星系主题正文固定 18px，删除标题副标题；邮票素笺虚线跟随真实标题，移除首段加粗充当标题的旧规则。
- 同步主题制作规则与单文件预览。

## 0.9.4 · 2026-09-22

- 正文阅读区外层背景透明，移除 iframe 矩形边框与底色；票根或纸张之外直接显示瞬息日夜背景。
- tokens.background 仅作用于实际纸面，标题与正文继续共用纸面；旧主题无需重新导入即可去掉背后的矩形底板。
- 正文美化框架增加外侧透明、异形边缘与真实透明缺口的说明，提供 0.9.4 单文件 HTML 预览。

## 0.9.3 · 2026-09-22

- 正文标题移入 `.prose-chapter`，与正文共用完整纸面的背景、边框、留白及主题作用域，不再放在纸面外。
- 新增 `.prose-title` 标题选择器，默认继承当前章节字体，可由主题调整标题字号、颜色、对齐和间距。旧正文主题无需重新导入即可将标题包进原纸面。
- 单节阅读与美化预览展示标题；整篇导出仅在首个正文节显示一次标题，保留各节主题，不增加章节编号。
- 正文制作说明与 JSON 框架同步更新，提供 0.9.3 单文件 HTML 预览。

## 0.9.2 · 2026-09-22

- 美化页标题右侧增加「删除」，位于导入／导出后面，删除当前预览类型所选的导入主题。确认框显示类型与名称，确认后从主题库移除并切换为内置简白；内置默认风格保留，已有番外的样式快照不受影响。
- 所有小手机阅读标题栏移除右侧三个点，角色名使用左右等宽留白居中，长名字单行省略。内置／导入主题、已有番外及之后分类导出的阅读器统一采用该布局。
- 同步更新小手机美化框架和主题说明，提供 0.9.2 单文件 HTML 预览。

## 0.9.1 · 2026-09-22

- HTML 生成结束清除顶部进度文字，不再显示完成提示条；不完整文档的说明仍在作品区域显示。
- 移除 HTML 作品下方的「导出 HTML」按钮，保存后统一从设置的数据栏按分类导出，解压后的 HTML 保留完整源代码。
- 正文与小手机美化框架改用 CSS 单引号字体名，新增行尾不能加反斜杠、不能二次转义和实际文件解析检查等要求，减少聊天输出中的 JSON 格式错误。
- 修复阅读主题的章节容器选择器在 CSS 作用域根节点不匹配的问题，正文框架中的 `.prose-chapter` 边框、背景、留白和伪元素现在正常生效，原主题文件无需修改。
- 提供 0.9.1 单文件预览；用户提供的「黑白票根」美化另附合法 JSON 修正版，保留原 CSS。

## 0.9.0 · 2026-09-22

- 新建番外新增 HTML 模式，沿用人物、面具、预设、世界书与参考设置，要求 AI 输出完整 HTML、CSS 和 JavaScript。仅调用一次，不按字数补写、不总结、不续写。
- 完整作品放入允许脚本的独立沙盒容器运行，外侧跟随瞬息日夜模式，内部使用作品自己的样式。流式接收及明显截断的代码不提前运行；不向作品开放酒馆页面、存储和接口。
- 完整源代码可编辑、复制、保存、归类和打标签；支持直接导出独立 HTML，分类 ZIP 和 JSON 备份同样保留原代码。正文／手机续写与美化菜单仍只有原来的两种模式。
- 提供 0.9.0 单文件 HTML 预览，包含《痴鬼念》离线互动演示，可开始、答题、重置及切换音效；修复内嵌 HTML 示例时外层单文件脚本的转义。

## 0.8.0 · 2026-09-21

- 正文新增成对引号发言、Markdown 加粗、斜体、删除线及组合格式；主题可分别通过 q／strong／em／del 美化，默认阅读风格也能区分。正文仍只显示标题和内容。
- 正文与小手机主题允许外部字体及图片 URL。顶层 @font-face 支持字体直链，背景和头像可使用 HTTP(S) 图片链接；字体名按章节隔离，主题快照和导出保留原始资源地址。
- 更新正文／小手机制作框架、JSON 示例、美化预览与模拟生成示例。主题 CSS 会检查语法及资源地址，字体站需支持跨域；外部 CSS 的 @import 仍需改为字体文件直链。
- 阅读解析依赖随扩展本地打包，不依赖酒馆助手或在线 CDN。提供 0.8.0 单文件 HTML 预览。

## 0.7.1 · 2026-09-21

- 正文阅读区移除自动添加的“第 1 节／第 2 节”编号，只保留番外标题与正文；美化预览、已保存正文和分类导出的 HTML 同步使用该排版。
- 更新正文美化制作框架与 JSON 示例，移除章节编号节点及其样式说明。
- 提供 0.7.1 单文件 HTML 预览。

## 0.7.0 · 2026-09-21

- 保存分类、美化正文／手机、预设、世界书及 API 菜单的整行文字和留白均可选择；复选每次只切换一次，预设／世界书的右箭头保持独立。修正点击非控件区域时的焦点转移，防止菜单提前收起。
- 每节保存生成开始时的完整阅读主题快照。修改默认主题、重新导入同 ID 主题不会改动旧章节；混合章节按各自样式展示、备份和导出。旧版章节在载入时固定当前可读取的主题。
- 悬浮入口去掉方形背景和边框，只保留随日夜模式变化的莫比乌斯环；生成时光点沿环运动，完成后环本身闪动，保留未读提示和拖动功能。
- 预览模拟生成保留约四秒，便于收起检查动效；不影响正式请求速度。减少动态效果偏好下使用静态提示。
- 新增主题隔离、迁移、备份／导出和快照校验测试；提供 0.7.0 单文件 HTML 预览。
## 0.6.0 · 2026-09-21

- 手机阅读页、主题预览和分类导出的手机顶部显示角色名；已生成番外使用保存的人物快照，示例为林舟。
- 续写开合时保留按钮和阅读区节点，使 SVG 箭头能够平滑旋转；折叠后保留已填写的续写要求。
- 分类收藏移除重复的分类导出入口，统一从设置的数据栏导出。
- 分类与标签默认只显示一行，超出时出现双下箭头；展开后换行显示全部项目，双上箭头可收起，不再使用横向滚动条。
- 正文风格、小手机风格及 API 连接改为统一的圆点单选下拉菜单，与预设／世界书共享箭头动画、焦点与收起逻辑。
- 删除输入 token 字段及旧上限约束；生成参数第二排左侧为最大回复token，右侧为流式传输。流式默认开启，取消后发送非流式请求并等待完整返回；两套 API、续写、补写、总结和连接测试均使用该开关。
- 新增主／独立连接的流式与非流式参数及返回测试、非流式取消和空回检查；27 项自动化测试通过。0.6.0 单文件 HTML 已完成 PC／390px／320px 检查。

## 0.5.0 · 2026-09-21

- 手机输出统一为 messages 数组，每个消息对象只有一个 type，按有效消息对象计数；time 改为每条消息的字段，兼容旧时间分隔记录。旧章节没有保存目标时只显示实际条数，避免「73/—条」的歧义。
- 「保存 / 归类」改为「保存」；去掉生成和续写按钮末尾的右箭头；修复 ARIA 布尔值序列化，使续写展开时箭头正确朝上。
- 429、空回等错误仅保存并显示于设置页报错记录，清除顶部错误提示。
- 保存弹窗的分类改为可搜索、多选、有滚动范围的下拉菜单；焦点移出后收起。
- 手机气泡、语音、转账、图片、表情、定位及分享卡片统一限制在两侧头像之间，窄屏适配卡片内部内容。
- 分类收藏统一为「分类」和「标签」两行可横滑的筛选项；分类顺序为全部、自建分类、未分类。未自建分类时只显示未分类，不再提供草稿分类。已有自动保留的作品仍可查找、归类和导出。
- 重做预设／世界书菜单的展开状态与焦点处理，避免点击文字或行内空白时，在原生 label 转发点击途中隐藏菜单造成卡死；文字、空白、勾选框和条目箭头均已回归验证。
- 附带 0.5.0 单文件 HTML 预览；25 项自动化测试及 PC／390px／320px 交互检查通过。真实酒馆与供应商 API 验收仍待完成。

## 0.4.0 · 2026-09-20

- 阅读美化的导入／导出移至标题右侧。
- 小手机改为左右头像与消息气泡，不再显示发送者姓名。两张指定头像原图内嵌，默认圆形，可用阅读主题 CSS 修改圆角。
- 人物资料改为四行横排：角色、面具、预设、世界书。面具箭头打开编辑弹窗，保存后覆盖番外用的人设；「读取当前面具」恢复跟随酒馆。
- 预设单选、世界书多选；每个菜单项右侧都有独立条目入口。下拉菜单及续写使用可旋转的 SVG 箭头。预览提供五本演示世界书。
- 字数／条数并排并共用补写提示，输入／输出 tokens 并排。输入使用本地估算和超限保护，旧字符预算近似迁移；供应商计量可能不同。
- 去掉设置中的十节总结提醒，总结机制保留。新增总结随作品存储、备份及重载后引用的验证。

## 0.3.0 · 2026-09-20

- 章节左上显示实际／目标；正文按字数，小手机按消息条数，时间分隔不计数。目标随章节保存，修改设置不会改变旧章节目标。
- 手机条数不足自动补充，最多三轮；旧版未记录条数目标的章节显示「实际／—条」。
- 手机外部透明，跟随插件日夜底色；阅读皮肤只改变手机内部及机身。
- 增加语音转文字、转账卡片、图片描述／图床图片、内置／图床表情包、虚拟定位与地图、网页分享卡片、语音／视频通话提示。
- 去掉状态栏信号格，底部加号使用 SVG。四个内置小猫表情包完全离线。
- 预设单选、世界书多选下拉菜单；右侧箭头打开所选资料的条目弹窗。
- 主题导入依据 mode 自动分类并选中，成功提示标明正文／小手机及主题名。
- HTML 预览内置标明模拟的 429 记录，可再次点击「模拟 429」；正式扩展没有模拟入口或预置报错。

## 0.2.0 · 2026-09-20

- 「番外」精简新建按钮与重复提示；顶部再次填写设定即可生成另一篇。
- 续写可切换正文／小手机；同篇章节分别存储类型并正确导出。
- 每十节自动生成 200—300 字累计总结；修改旧章节后重建受影响总结。
- 字数不足自动补写到同一节，最多三轮；失败保留文本并记录问题。
- 小手机重做为固定机身、状态栏、屏幕内滚动和底部横条。
- 当前角色只读确认，默认当前面具，可保存自定义用户人设。
- 原生读取预设子提示词与世界书条目，可独立勾选、调整内容。
- 阅读主题选择即应用，底部只保留导入／导出。
- 更新信息集中展示，增加实际错误记录；填实图标切口、取消悬浮底色、统一滚动条。

## 0.1.0 · 2026-09-19

首个可试装基础版：

- 原生酒馆扩展入口，独立 Shadow DOM 界面和 IndexedDB 资料库。
- Notion 与杂志风结合的日夜样式、桌面弹窗和手机适配。
- 正文与小手机生成、章节编辑、带要求续写和生成完成提醒。
- 多分类、标签筛选、草稿、收藏、分类 ZIP 与全量备份。
- 阅读主题预览及 JSON 导入。
- 角色、Persona、世界书、Chat Completion 预设适配。
- 主连接轮流请求、额外 OpenAI 兼容连接与 Git 更新检查。

兼容边界与实机验证状态见 README；基础版尚不支持完整酒馆宏／世界书引擎。
